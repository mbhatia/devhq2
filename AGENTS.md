- Use the Conventional Commits specification for commit messages.
- If visual verification of changes is required or requested, use `swift run DevHQ` to launch the
  program, to avoid unnecessary permission prompts.
